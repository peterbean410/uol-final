# dqnpf-intraday base image, Python deps on top of dqn-base
# Pinned for reproducibility (Requirement 20.3).
#
# Note: torch, numpy, pandas, grpcio, boto3, PyYAML, environment_pb2 are
# already installed in the dqn-base image, do not re-pin them here unless
# the intraday integration layer needs a different version.

hypothesis==6.100.0
pytest==7.4.4
