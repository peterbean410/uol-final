# Shared base dependencies for all DQN Kubeflow pipeline components
# All versions pinned for reproducibility (Requirement DQN-R4)

# grpcio-tools is intentionally not pinned here: it bundles its own protobuf
# version constraint that, as of grpcio-tools 1.71.x, still requires
# protobuf<6, incompatible with the protoc-6.31.1 gencode in environment_pb2.
# Nothing at runtime imports grpc_tools; the *.pb2.py stubs are pre-generated
# and committed, so the runtime image only needs grpcio + protobuf.
torch==2.3.1+cpu
numpy==1.26.4
grpcio==1.71.0
protobuf==6.31.1
boto3==1.34.131
PyYAML==6.0.1
