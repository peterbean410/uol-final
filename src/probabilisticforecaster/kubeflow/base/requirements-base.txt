# Shared base dependencies for all Kubeflow pipeline components
# All versions pinned for reproducibility (Requirement 2.1)
#
# torch is intentionally NOT pinned here: its CPU wheel spec is arch-specific
# (x86_64 uses the `+cpu` local version, aarch64 the plain version) and is
# installed by Dockerfile.base from TORCH_{AMD64,ARM64}_SPEC before this file.

numpy==1.26.4
pandas==2.2.2
boto3==1.34.131
PyYAML==6.0.1
